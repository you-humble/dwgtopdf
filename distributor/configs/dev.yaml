base_dir: "temp"
grpc_addr: "converter:50051"
queue_capacity: 64
pool_size: 16
task_ttl: 60s
task_cleanup_interval: 60s
conversion_timeout: 10s

redis:
  addr: "redis:6379"
  password: "super_secret_password"
  db: 0

minio:
  endpoint: "minio:9000"
  access_key_id: "ebat"
  secret_access_key: "kopat322"
  use_ssl: false
  bucket: "dwg-files"

nats:
  url: "nats://nats:4222"
  queue_name: "dwg-converter"
  max_reconnects: 10
  subject: "dwg.conversion"
